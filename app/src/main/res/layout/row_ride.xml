<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	tools:ignore="RtlSymmetry"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:orientation="vertical"
	android:paddingTop="8dp"
	android:paddingBottom="8dp"
	android:paddingStart="16dp"
	android:paddingLeft="16dp"
	android:paddingEnd="16dp"
	android:paddingRight="16dp"
	android:background="@drawable/row_ride_selector">
	<TextView
		android:id="@+id/date"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:textSize="12sp"
		android:textColor="@color/secondary"
		android:paddingBottom="4dp"/>
	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:orientation="horizontal">
		<TextView
			android:id="@+id/mistakes"
			android:drawablePadding="4dp"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:paddingRight="8dp"
			android:paddingEnd="8dp"
			android:textColor="@color/foreground"
			app:drawableLeftCompat="@drawable/ic_mistakes"
			app:drawableStartCompat="@drawable/ic_mistakes"/>
		<TextView
			android:id="@+id/distance"
			android:drawablePadding="4dp"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:paddingRight="8dp"
			android:paddingEnd="8dp"
			android:textColor="@color/foreground"
			app:drawableLeftCompat="@drawable/ic_distance"
			app:drawableStartCompat="@drawable/ic_distance"/>
		<TextView
			android:id="@+id/duration"
			android:drawablePadding="4dp"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:paddingRight="8dp"
			android:paddingEnd="8dp"
			android:textColor="@color/foreground"
			app:drawableLeftCompat="@drawable/ic_duration"
			app:drawableStartCompat="@drawable/ic_duration"/>
		<TextView
			android:id="@+id/average"
			android:drawablePadding="4dp"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:paddingRight="8dp"
			android:paddingEnd="8dp"
			android:textColor="@color/foreground"
			app:drawableLeftCompat="@drawable/ic_average"
			app:drawableStartCompat="@drawable/ic_average"/>
		<TextView
			android:id="@+id/score"
			android:layout_width="0dp"
			android:layout_height="wrap_content"
			android:layout_weight="1"
			android:layout_gravity="end"
			android:gravity="end"
			android:textColor="@color/foreground"/>
	</LinearLayout>
</LinearLayout>
